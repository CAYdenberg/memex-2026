## GhostVM

- How do I make vmctl accessible on the CLI? Is it somewhere on the hard drive and I just need to add it to the PATH?
- Is the idea to install Claude on the host and have it manipulate the VM via the command line?
- Could we walk through a couple of scenarios? Imagine I'm building a Node server or an electron app (with AI tools), how would I use Ghost to help?